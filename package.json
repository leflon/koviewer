{
	"scripts": {
		"dev": "NODE_ENV=development cd src && bun --hot server.ts",
		"start": "cd dist && bun server.js",
		"build": "bun build src/server.ts --target=bun --outdir dist --minify"
	},
	"dependencies": {
		"@types/bun": "^1.3.2",
		"leaflet": "^1.9.4",
		"leaflet.sync": "^0.2.4",
		"topojson-client": "^3.1.0"
	},
	"devDependencies": {
		"@types/geojson": "^7946.0.16",
		"@types/leaflet": "^1.9.21",
		"@types/topojson-client": "^3.1.5"
	}
}
